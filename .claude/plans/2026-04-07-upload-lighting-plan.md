# 计划：拣货数据上传增加亮灯功能

- 日期：2026-04-07
- 目标文件：data/dongxinheping/api/东信和平/亮灯对接/拣货数据上传.ms

## 目标
- 上传拣货数据成功后，触发亮灯。
- 颜色规则与拣货完成保持一致：多人作业 BLUE，单人作业 GREEN。
- 亮灯调用使用异步，不阻塞主流程返回。

## 实施步骤
1. 在脚本中引入 turnOnLedByArticleId。
2. 从本次上传明细中收集待亮灯库位，去重且仅保留 status != 1。
3. 统计波次参与用户数 count(distinct user_id) 并计算颜色。
4. 事务提交后异步调用亮灯接口。
5. 增加日志与测试样例，便于定位执行结果。

## 风险与控制
- 仅在事务提交后触发灯控，避免事务回滚但灯已亮。
- 异步触发失败仅记录日志，不影响上传主流程成功返回。
