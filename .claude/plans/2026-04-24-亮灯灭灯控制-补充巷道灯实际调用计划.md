# 2026-04-24 亮灯灭灯控制补充巷道灯实际调用计划

## 目标
- 为 `亮灯灭灯控制.ms` 增加巷道灯真实开/关调用，消除“只更新状态不下发设备命令”。

## 范围
- 仅修改 `data/dongxinheping/api/东信和平/亮灯控制/亮灯灭灯控制.ms`。

## 实施
1. 导入 `turnOnAisleLedByLotNo` 与 `turnOffAisleLedByLotNo`。
2. 复用现有派发结果：
   - 关灯：`turnOffDispatchCodes`
   - 开灯：`turnOnMultiDispatchCodes + singleColorLocationMap` 去重集合
3. 先关后开，保持当前派发顺序一致。
