# 计划：PDA绑定页面统一入口导航

- 日期：2026-04-07
- 目标：将 led-mapping 与 location-label-mapping 通过一个入口页面统一导航

## 目标
- 新增一个 PDA 入口页面，提供两个导航入口：
  - 库位-巷道灯绑定（led-mapping）
  - 库位-电子标签绑定（location-label-mapping）
- 不改动原有两个业务页面逻辑，降低回归风险。

## 实施步骤
1. 新建入口页面 `mapping-entry.html`。
2. 设计移动端友好的入口卡片与按钮跳转。
3. 校验链接路径与文案可读性。
4. 补充变更日志、里程碑、测试报告。
5. 执行编译验证。
