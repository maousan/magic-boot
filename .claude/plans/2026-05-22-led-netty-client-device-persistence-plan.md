# LED Netty客户端设备保存计划

## 目标

- 当 Netty 客户端上报包含 MAC 的数据后，将设备信息保存到 `t_led_device`。
- 新 MAC 自动插入，已存在 MAC 更新 IP。

## 假设

- TCP 连接建立时只能拿到远端地址，无法可靠拿到设备 MAC。
- 设备 MAC 来自客户端首次上报的数据帧；因此保存动作发生在解析到 MAC 后。

## 验证

- 新增持久化服务单元测试，验证插入和更新 SQL 被调用。
- 更新 handler 测试，验证解析到 MAC/IP 后会触发设备上报回调。
- 执行插件测试或最小相关测试。
