# 定时任务执行日志查询功能计划

## 目标

在定时任务管理页面增加执行日志查询能力，用户可以从任务列表直接查看某个任务的最近执行记录。

## 范围

1. 在定时任务列表操作列增加“日志”按钮。
2. 点击后打开弹窗，调用现有 `/magic/job/{jobId}/history` 接口查询执行历史。
3. 弹窗内以表格展示执行状态、触发类型、开始时间、结束时间、耗时、执行结果和异常信息。
4. 支持按页切换查询，避免一次性加载过多日志。

## 验证

1. 前端 TypeScript 编译通过。
2. 静态资源重新构建成功。
