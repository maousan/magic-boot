# 东信和平数据库查询结果渲染修复

## 背景

数据库管理查询接口已返回数据，但前端结果表格未正常渲染。

## 变更

- 为查询结果表格补充 `height: 100%`，避免 Naive UI `flex-height` 在 flex 容器中表体高度异常。
- 查询响应增加兜底归一化，兼容直接返回数组或标准 `{ list, rowCount, hasMore, duration_ms }` 结构。

## 影响

- 不改变查询接口路径和业务逻辑。
- 修改前端静态页面，需要重新编译前端并重新打包插件后部署到插件目录。
