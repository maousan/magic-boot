# 库位列表导入结果展示调整

## 背景

库位列表页面导入文件后，原先会在列表下方保留一块“导入结果”区域。用户反馈该结果不需要作为页面固定区域展示，只需要完成导入时弹窗提示。

## 变更

- 移除库位列表页面内嵌的“导入结果”卡片。
- 导入完成后使用 Naive UI dialog 展示导入总行数、成功数、失败数。
- 如果存在失败明细，在 dialog 内展示错误行号和错误信息。

## 影响范围

- 仅影响 `dongxinheping-admin` 库位列表页面的导入结果展示方式。
- 导入接口、列表查询、模板下载和手动添加逻辑未调整。
