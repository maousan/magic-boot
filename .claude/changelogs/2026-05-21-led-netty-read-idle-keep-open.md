# LED Netty读空闲连接保持

## 变更内容

- `LedNettyServerHandler` 在触发 `READER_IDLE` 时不再主动关闭客户端连接。
- 读空闲日志从 `warn` 降为 `debug`，避免设备只上报一次后持续产生告警。

## 原因

当前 LED 设备连接后只上报一次，后续主要等待服务端主动下发消息。原逻辑会在 120 秒未读到数据后关闭连接，不符合该设备通信模型。

## 影响

- 设备连接会持续保留，便于后续通过 `sendToClient` 主动下发。
- 断线清理仍依赖 TCP 连接关闭、异常事件或服务停止重置。
