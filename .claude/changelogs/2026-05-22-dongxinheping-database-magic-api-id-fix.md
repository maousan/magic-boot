# 东信和平数据库管理 magic-api 元数据修复

## 背景

magic-api 分组树中“数据库管理”不显示。排查发现该分组及其 3 个接口的元数据 id 与“库位/批次EPC绑定”分组及接口重复，运行时缓存按 id 覆盖，导致数据库管理分组被挤掉。

## 变更

- 为“数据库管理”分组更换唯一 id。
- 为“获取表列表”“执行查询”“执行写操作”更换唯一接口 id。
- 同步更新 3 个接口的 groupId 指向新的“数据库管理”分组 id。

## 影响

- 不修改接口路径、请求方法和脚本业务逻辑。
- 需要重载 magic-api 资源或重启服务后生效。
