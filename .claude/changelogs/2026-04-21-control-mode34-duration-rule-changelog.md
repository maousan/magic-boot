# 2026-04-21 control接口mode3/4时长约束变更日志

## 变更内容
- 文件：`data/dongxinheping/api/东信和平/亮灯控制/亮灯灭灯控制.ms`
- 新规则：当 `mode` 为 `3` 或 `4` 时，`duration` 必填且仅允许 `0` 或 `0s`。
- 归一化：当传入 `0s` 时，统一转换为 `0` 再下发。
- 兼容：当 `mode` 为 `1` 或 `2` 时，仍沿用原有亮灯时长枚举校验，不受本次规则影响。

## 影响
- 避免关灯场景传入非预期时长。
- 保持开灯场景时长能力不变。
