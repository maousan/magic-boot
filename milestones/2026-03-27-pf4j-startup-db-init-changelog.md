# PF4J 启动补录 DB 变更日志

## 变更日期
- 2026-03-27

## 变更类型
- 功能调整

## 变更内容
- 新增配置项 `plugin.init-sync-on-startup`（默认 `true`）。
- 新增启动初始化器：应用启动后执行运行态插件增量入库。
- 新增服务方法：仅补录 DB 缺失插件，不覆盖已有记录。
- `application.yml` 增加 `plugin.init-sync-on-startup` 配置示例。
- 新增单元测试覆盖补录与跳过场景。

## 兼容性
- 对已有接口无破坏性变更。
- 可通过配置开关关闭启动补录逻辑。

