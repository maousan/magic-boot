# 里程碑：文件列表 parentPath 未命中返回空

日期：2026-03-26  
范围：`获取文件列表.ms`（文件浏览）

## 变更内容（已完成）
- 当请求传入 `parentPath` 且该路径不是根路径 `/`，如果数据库中找不到对应父节点，接口直接返回空列表：
  - `total: 0`
  - `list: []`
- 不再回退到“根目录查询”，避免出现“路径不存在却返回根目录数据”的误导行为。

## 兼容说明
- `parentPath = "/"` 仍按根目录语义处理，不受此变更影响。
