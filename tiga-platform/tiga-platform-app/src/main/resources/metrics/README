## =========================================性能看板=========================================
如何在 Grafana 中应用
	1、添加数据源：确保你的 Grafana 已连接到 Prometheus。

	2、导入 JSON：
		  点击 Grafana 左侧侧边栏的 Dashboards。
		  点击右侧的 New 按钮 -> Import。
		  将 JSON 粘贴到文本框中，点击 Load。

	3、查看效果：你将看到一个深色主题的仪表盘，实时显示你的脚本引擎性能。

特别说明：PromQL 计算逻辑
	1、缓存命中率：我们使用了 sum(tiga_groovy_cache_hits_total)。这是一个累加值（Counter），
	   Grafana 会展示总的命中与未命中比例。
	
	2、平均耗时：公式为 rate(sum_seconds) / rate(count)。这是 Prometheus 统计平均响应时间的标准做法。

	3、分位数 (Quantiles)：我们在 recordExecution 中开启了 publishPercentileHistogram()。
	   这会生成特殊的 _bucket 指标，允许 Prometheus 计算“95% 的用户耗时都在 X 毫秒以下”。

调试建议，如果在 Grafana 中看不到数据，请检查以下两点：

	1、接口访问：
		在浏览器访问 http://localhost:8080/groovy/metrics，搜索 tiga_groovy，确认数据已经产出。
		在浏览器访问 http://localhost:8080/magic/metrics，搜索 tiga_magic，确认数据已经产出。

	2、Prometheus 采集：检查 Prometheus 的 targets 页面，确认你的服务状态是 UP。
	
## =========================================告警使用=========================================

如何在 Prometheus 中启用
	1、保存文件：将上述内容保存为 /etc/prometheus/alert_rules.yml。

	2、修改主配置：在 prometheus.yml 中添加如下引用：
		rule_files:
		  - "alert_rules.yml"
		  
	3、热加载配置： 
		执行 curl -X POST http://localhost:9090/-/reload 或者重启 Prometheus 容器。

告警逻辑解读
	1、rate(..._sum) / rate(..._count)：这是计算平均耗时的标准 PromQL 语法。
		它可以动态计算过去 1 分钟内的移动平均值，避免瞬时尖峰导致的误报。

	2、for: 2m：
		表示异常必须持续 2 分钟才会触发告警，这有效过滤了脚本首次冷启动（需要编译）时产生的偶然性长耗时。

	3、$labels.scriptId：
		由于我们在 Micrometer 埋点时添加了 tag("script_id", scriptId)，告警信息中会直接告诉你是哪一个特定的脚本出了问题，方便快速定位。

